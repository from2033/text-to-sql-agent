
  # Text-to-SQL Agent

  This is a code bundle for Text-to-SQL Agent. The original project is available at https://www.figma.com/design/8SVRIeQJboxBkXHObpDBzU/Text-to-SQL-Agent.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  # text-to-sql-agent
